# Build
target/
research/out/
web/.next/
web/node_modules/
web/out/
.DS_Store
*.swp

# Editor / local tooling
.claude/

# Local agent scratch
CLAUDE.md
research/blog-drafts/

# Fuzzer working data — corpora regenerate from committed vectors
# (`just fuzz-seeds-pq`) and grow without bound during runs. Tracked
# subdirs (fuzz/Cargo.toml, fuzz/fuzz_targets/, fuzz/scripts/) hold the
# things needed to reproduce; fuzz/seeds/ + fuzz/runs/ are derived.
fuzz/seeds/
fuzz/runs/
fuzz/corpus/
fuzz/artifacts/
